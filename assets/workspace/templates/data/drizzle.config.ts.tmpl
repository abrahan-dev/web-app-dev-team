import { defineConfig } from "drizzle-kit";

export default defineConfig({
  dialect: "sqlite",
  schema: "./src/contexts/*/infrastructure/persistence/schema.ts",
  out: "./drizzle",
  dbCredentials: { url: "./.data/{{applicationName}}.sqlite" },
});
